<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Needed for voice dictation (Vosk recognizes speech entirely on-device;
         this permission only lets the app read the microphone stream, no
         audio ever leaves the phone). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- allowBackup="false": the whole point of this app is that nothing
         leaves the device. Left at the template's "true", Android's own
         backup service would upload the notes database and every
         preferences file (including the PIN salts and hashes) to the
         user's Google Drive — no INTERNET permission required, since the
         transfer is done by a system process, not by us.
         The two rules files below stay wired up as defence in depth: they
         spell out the same exclusions, so flipping this attribute back on
         by accident still wouldn't hand anything over. -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JustNotes">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.JustNotes"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lets us share an exported note as a file attachment (rather than
             just pasted text) without exposing the app's storage directly:
             other apps only ever see this specific content:// URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>