<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Matches the "exports" subfolder NoteExporter writes to, inside the
         app's private cache directory. -->
    <cache-path name="exports" path="exports/" />
</paths>
