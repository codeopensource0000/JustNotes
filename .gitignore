*.iml
.gradle
# Regenerated by Android Studio on open; nothing here is needed to build
# from the command line or to open the project fresh.
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing: credentials and the keystore itself. Never commit these —
# anyone holding them can publish an update that Android accepts as genuine.
keystore.properties
*.jks
*.keystore

# Claude Code local tool config for this directory — not part of the app.
.claude/
